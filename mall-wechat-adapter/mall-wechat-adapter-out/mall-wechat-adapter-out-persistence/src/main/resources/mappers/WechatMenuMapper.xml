<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.wechat.adapter.out.persistence.mapper.WechatMenuMapper">

    <!-- 根据父菜单ID查询子菜单 -->
    <select id="findByParentId" resultType="com.mall.wechat.domain.model.menu.WechatMenu">
        SELECT menu_id, click_msg_type, menu_key, menu_name, menu_reply_data, 
               menu_sort, menu_state, menu_type, menu_view_url, parent_id, parent_name,
               created_at, created_by, updated_at, updated_by, is_deleted
        FROM wechat_menu 
        WHERE parent_id = #{parentId} AND is_deleted = 0
        ORDER BY menu_sort ASC, created_at ASC
    </select>

    <!-- 根据菜单状态查询菜单 -->
    <select id="findByMenuState" resultType="com.mall.wechat.domain.model.menu.WechatMenu">
        SELECT menu_id, click_msg_type, menu_key, menu_name, menu_reply_data, 
               menu_sort, menu_state, menu_type, menu_view_url, parent_id, parent_name,
               created_at, created_by, updated_at, updated_by, is_deleted
        FROM wechat_menu 
        WHERE menu_state = #{menuState} AND is_deleted = 0
        ORDER BY menu_sort ASC, created_at ASC
    </select>

</mapper>